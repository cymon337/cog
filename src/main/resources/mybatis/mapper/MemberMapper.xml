<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cymon.cog.member.dao.MemberMapper">

    <select id="selectByMemberId" resultType="MemberDto">
        SELECT
            MEMBER_ID
            ,  MEMBER_PWD
            ,  MEMBER_EMAIL
            ,  MEMBER_MOBILE
            ,  MEMBER_ROLE
            ,  MEMBER_REG_DATE
            ,  MEMBER_STATUS
        FROM MEMBER
        WHERE MEMBER_ID = #{memberId}
    </select>

    <select id="selectByEmail" resultType="MemberDto">
        SELECT
            MEMBER_ID
            ,  MEMBER_PWD
            ,  MEMBER_EMAIL
            ,  MEMBER_MOBILE
            ,  MEMBER_ROLE
            ,  MEMBER_REG_DATE
            ,  MEMBER_STATUS
        FROM MEMBER
        WHERE MEMBER_EMAIL = #{memberEmail}
    </select>

    <select id="selectByMobile" resultType="MemberDto">
        SELECT
            MEMBER_ID
             ,  MEMBER_PWD
             ,  MEMBER_EMAIL
             ,  MEMBER_MOBILE
             ,  MEMBER_ROLE
             ,  MEMBER_REG_DATE
             ,  MEMBER_STATUS
        FROM MEMBER
        WHERE MEMBER_MOBILE = #{memberMobile}
    </select>

    <select id="findByEmail" resultType="MemberDto">
        SELECT
            MEMBER_ID
            ,  MEMBER_PWD
            ,  MEMBER_EMAIL
            ,  MEMBER_MOBILE
            ,  MEMBER_ROLE
            ,  MEMBER_REG_DATE
            ,  MEMBER_STATUS
        FROM MEMBER
        WHERE MEMBER_EMAIL = #{memberEmail}
    </select>

    <select id="findByMobile" resultType="MemberDto">
        SELECT
            MEMBER_ID
             ,  MEMBER_PWD
             ,  MEMBER_EMAIL
             ,  MEMBER_MOBILE
             ,  MEMBER_ROLE
             ,  MEMBER_REG_DATE
             ,  MEMBER_STATUS
        FROM MEMBER
        WHERE MEMBER_MOBILE = #{memberMobile}
    </select>

    <select id="findByMemberId" resultType="MemberDto">
        SELECT
            MEMBER_ID
            ,  MEMBER_PWD
            ,  MEMBER_EMAIL
            ,  MEMBER_MOBILE
            ,  MEMBER_ROLE
            ,  MEMBER_REG_DATE
            ,  MEMBER_STATUS
        FROM MEMBER
        WHERE MEMBER_ID = #{memberId}
    </select>

    <insert id="insertMember" parameterType="MemberDto">
        INSERT INTO MEMBER
        (
            MEMBER_ID
            ,  MEMBER_PWD
            ,  MEMBER_EMAIL
            ,  MEMBER_MOBILE
        )
        VALUES
        (
            #{memberId},
            #{memberPwd},
            #{memberEmail},
            #{memberMobile}
        )
    </insert>

</mapper>
